### 2.1 API 测试用例

#### 2.1.1 用户模块

| 接口 | 方法 | 路径 | 功能 |
|------|------|------|------|
| 用户注册 | POST | `/api/users/register` | 创建新用户 |
| 用户登录 | POST | `/api/users/login` | 用户认证 |
| 获取用户信息 | GET | `/api/users/profile` | 获取当前用户信息 |
| 更新用户信息 | PUT | `/api/users/profile` | 更新用户资料 |

**注册测试**
```bash
curl -X POST http://localhost:3001/api/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "email": "test@example.com",
    "password": "123456",
    "phone": "13800138000"
  }'
```

**登录测试**
```bash
curl -X POST http://localhost:3001/api/users/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "123456"
  }'
```

#### 2.1.2 商品模块

| 接口 | 方法 | 路径 | 功能 |
|------|------|------|------|
| 获取商品列表 | GET | `/api/products` | 获取商品列表（支持分页、筛选、排序） |
| 获取商品详情 | GET | `/api/products/:id` | 获取单个商品详情 |
| 创建商品 | POST | `/api/products` | 创建新商品（需要认证） |
| 更新商品 | PUT | `/api/products/:id` | 更新商品信息 |
| 删除商品 | DELETE | `/api/products/:id` | 删除商品 |

**获取商品列表**
```bash
curl "http://localhost:3001/api/products?page=1&limit=10&category=electronics"
```

**获取商品详情**
```bash
curl http://localhost:3001/api/products/1
```

#### 2.1.3 订单模块

| 接口 | 方法 | 路径 | 功能 |
|------|------|------|------|
| 创建订单 | POST | `/api/orders` | 创建新订单 |
| 获取订单列表 | GET | `/api/orders` | 获取当前用户订单列表 |
| 获取订单详情 | GET | `/api/orders/:id` | 获取单个订单详情 |
| 更新订单状态 | PUT | `/api/orders/:id/status` | 更新订单状态 |

#### 2.1.4 二手交易模块

| 接口 | 方法 | 路径 | 功能 |
|------|------|------|------|
| 获取二手商品 | GET | `/api/secondhand` | 获取二手商品列表 |
| 发布二手商品 | POST | `/api/secondhand` | 发布二手商品 |
| 获取二手商品详情 | GET | `/api/secondhand/:id` | 获取二手商品详情 |

#### 2.1.5 评价模块

| 接口 | 方法 | 路径 | 功能 |
|------|------|------|------|
| 创建评价 | POST | `/api/evaluations` | 创建商品评价 |
| 获取商品评价 | GET | `/api/evaluations/product/:productId` | 获取商品评价列表 |

### 2.2 测试工具推荐

- **Postman**：API 测试工具
- **curl**：命令行测试工具
- **jest/supertest**：自动化测试框架

### 2.3 健康检查

```bash
curl http://localhost:3001/api/health
# 期望响应: {"status": "ok"}
```

---

## 3. 小学期测试计划补充

### 3.1 测试目标

小学期测试重点从原有功能正确性扩展到非功能属性验证，包括接口稳定性、回归测试、性能测试、安全测试和部署验证。

测试目标：

- 验证注册、登录、商品、订单、店铺、评价、聊天等核心功能可用。
- 验证重构后接口兼容原有前端调用。
- 验证高频接口在一定并发下保持可接受响应时间。
- 验证关键接口具备基础安全防护。
- 为最终汇报提供测试记录和缺陷闭环证据。

### 3.2 测试范围

| 类型 | 范围 | 负责人 |
| --- | --- | --- |
| 功能测试 | 注册登录、商品浏览、下单、二手发布、评价、店铺管理 | 赵紫嫣 |
| 接口测试 | 后端 REST API | 赵紫嫣、浦灵一 |
| 回归测试 | 重构后核心业务流程 | 全体 |
| 性能测试 | 登录、商品列表、商品详情、创建订单 | 剧博洋 |
| 安全测试 | 鉴权、越权、上传、限流、参数校验 | 浦灵一 |
| 部署验证 | CodeArts 流水线、本地环境启动 | 陈子正 |

### 3.3 回归测试用例

| 编号 | 用例 | 步骤 | 期望 |
| --- | --- | --- | --- |
| REG-01 | 用户注册登录 | 注册新用户并登录 | 返回 token，可进入个人中心 |
| REG-02 | 商品浏览 | 打开首页和商品列表 | 商品正常展示，分页可用 |
| REG-03 | 商品详情 | 点击商品进入详情页 | 展示价格、库存、卖家、评价信息 |
| REG-04 | 创建订单 | 登录后选择商品并下单 | 订单创建成功，库存减少 |
| REG-05 | 二手发布 | 发布二手商品 | 商品出现在二手列表中 |
| REG-06 | 店铺管理 | 创建或更新店铺信息 | 店铺信息保存成功 |
| REG-07 | 评价商品 | 对订单商品提交评价 | 评价列表可查询 |
| REG-08 | 聊天议价 | 买家与卖家发送消息 | 消息记录可查询 |

### 3.4 性能测试计划

核心接口：

| 接口 | 并发 | 持续时间 | 目标 |
| --- | --- | --- | --- |
| GET /api/products | 50 | 3 分钟 | 错误率低于 1%，P95 小于 1000ms |
| GET /api/products/:id | 50 | 3 分钟 | 错误率低于 1%，P95 小于 1000ms |
| POST /api/users/login | 20 | 2 分钟 | 错误率低于 1%，P95 小于 1500ms |
| POST /api/orders | 20 | 2 分钟 | 不出现负库存 |

记录指标：

- QPS。
- 平均响应时间。
- P95 响应时间。
- 错误率。
- 数据库连接是否稳定。
- 服务是否出现异常日志。

### 3.5 安全测试计划

| 编号 | 测试项 | 方法 | 期望 |
| --- | --- | --- | --- |
| SEC-01 | 未登录访问 | 不带 token 请求个人资料 | 返回 401 |
| SEC-02 | 越权访问 | 用户访问他人订单或商品管理接口 | 返回 403 |
| SEC-03 | 弱密码注册 | 使用过短密码注册 | 返回参数错误 |
| SEC-04 | 高频登录 | 短时间重复登录 | 触发限流 |
| SEC-05 | 非法上传 | 上传脚本文件 | 上传失败 |
| SEC-06 | 非法 CORS 来源 | 使用未授权 Origin 请求 | 拒绝跨域 |

### 3.6 缺陷记录模板

| 缺陷编号 | 标题 | 严重程度 | 发现人 | 负责人 | 状态 | 修复说明 |
| --- | --- | --- | --- | --- | --- | --- |
| BUG-001 | 待填写 | 高/中/低 | 待填写 | 待填写 | 待修复 | 待填写 |

### 3.7 测试验收标准

- 核心回归测试用例全部通过。
- 关键接口测试有记录。
- 至少完成 3 个核心接口的性能测试。
- 安全测试覆盖鉴权、越权、上传和限流。
- 发现的严重缺陷完成修复或说明规避措施。
- 测试结果可作为最终汇报材料。

### 3.8 已补充自动化测试

后端已补充 Node.js 内置测试脚本：

```bash
cd backend
npm test
```

当前自动化测试覆盖：

- 信用等级与信用分规则。
- 低评分惩罚规则。
- 发货信用加减分规则。
- 卖家订单 DTO 过滤逻辑。
- 安全响应头。
- 基础限流中间件。
- 生产环境 JWT 密钥校验。
- 注册参数校验。
- 注册、登录、资料、地址、店铺认证、商品、二手商品、聊天议价、订单、评价和上传白名单校验 API 链路。
- Playwright 浏览器注册、搜索商品、商品详情、加购、结算、新增地址、提交订单、店铺认证、店铺编辑、二手发布、订单评价和聊天议价处理流程。

最近一次本地执行结果：

```text
npm --prefix backend test: pass 9, skipped 1
npm --prefix backend run test:api: pass 1
npm --prefix frontend run test:e2e: pass 4
fail 0
```

### 3.9 性能测试脚本

已新增 k6 压测脚本：

```text
04_tests/performance/k6-core-flow.js
```

执行方式：

```bash
k6 run -e BASE_URL=http://localhost:3001 04_tests/performance/k6-core-flow.js
```

脚本覆盖：

- 商品列表接口。
- 健康检查接口。
- P95 响应时间阈值。
- 错误率阈值。

---
