### 1.1 项目概述

本项目是一个购物与二手交易平台，包含前后端两个独立服务：

| 服务 | 技术栈 | 端口 | 目录 |
|------|--------|------|------|
| 后端 | Node.js + Express + MySQL | 3001 | `backend/` |
| 前端 | React + Vite + Redux | 5173 | `frontend/` |

### 1.2 前置条件

- [Railway 账户](https://railway.app/)
- GitHub/GitLab 仓库（已包含项目代码）
- MySQL 知识（本地开发调试用）

### 1.3 Railway 部署步骤

#### 1.3.1 部署后端服务

**步骤 1：创建新项目**
1. 登录 Railway
2. 点击 "New Project" → "Deploy from GitHub repo"
3. 选择项目仓库，指定 `backend/` 目录

**步骤 2：配置环境变量**

在 Railway 项目的 "Variables" 页面添加以下环境变量：

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `PORT` | `3001` | 服务端口 |
| `DB_HOST` | Railway MySQL 提供的主机名 | 自动生成 |
| `DB_PORT` | `3306` | MySQL 端口 |
| `DB_USER` | Railway MySQL 提供的用户名 | 自动生成 |
| `DB_PASSWORD` | Railway MySQL 提供的密码 | 自动生成 |
| `DB_NAME` | Railway MySQL 提供的数据库名 | 自动生成 |
| `JWT_SECRET` | 随机字符串（建议32位以上） | 用于JWT签名 |
| `JWT_EXPIRES_IN` | `30d` | Token有效期 |
| `CORS_ORIGIN` | 前端域名（如 `https://xxx.up.railway.app`） | 允许的跨域来源 |
| `NODE_ENV` | `production` | 生产环境 |

**步骤 3：添加 MySQL 数据库**
1. 在 Railway 项目中添加 "MySQL" 插件
2. Railway 会自动配置数据库连接信息

**步骤 4：启动部署**

Railway 会自动读取 `railway.json` 并执行部署：
```json
{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

#### 1.3.2 部署前端服务

**步骤 1：创建新项目**
1. 点击 "New Project" → "Deploy from GitHub repo"
2. 选择项目仓库，指定 `frontend/` 目录

**步骤 2：配置环境变量**

添加以下环境变量：

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `PORT` | Railway 分配的端口 | 自动分配 |
| `VITE_API_URL` | 后端服务域名（如 `https://xxx.up.railway.app`） | 后端API地址 |

**步骤 3：启动部署**

Railway 会自动读取 `railway.json` 并执行部署：
```json
{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run preview -- --host 0.0.0.0 --port ${PORT}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

### 1.4 本地开发环境搭建

#### 1.4.1 后端环境

```bash
cd backend

# 安装依赖
npm install

# 创建环境变量文件
cp .env.example .env

# 修改 .env 文件，配置本地 MySQL
# DB_HOST=localhost
# DB_USER=root
# DB_PASSWORD=your_password
# DB_NAME=shopping_platform

# 启动开发服务器
npm run dev
```

#### 1.4.2 前端环境

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器（已配置代理到 localhost:3001）
npm run dev
```

### 1.5 数据库初始化

```bash
# 在 backend 目录执行
npm run seed
```

此命令会执行 `seedDemoProducts.js`，初始化示例商品数据。

---

## 2. 小学期 CodeArts 开发平台部署与管理补充

### 2.1 平台定位

小学期阶段继续使用华为云 CodeArts 作为开发平台。当前项目部署目标选择为 A：CodeArts 主要用于项目管理、代码仓库、敏捷看板和基础流水线验证，系统运行演示优先使用本地或测试环境。

CodeArts 必做内容：

- 创建 CodeArts 项目。
- 导入或同步当前 Git 仓库。
- 使用 Scrum 或看板管理任务。
- 建立两个 Sprint。
- 配置基础 CI 流水线。
- 保留过程截图和构建记录。

### 2.2 CodeArts 项目创建

1. 登录华为云 CodeArts：

```text
https://devcloud.cn-north-4.huaweicloud.com/home
```

2. 新建项目，项目名称建议为：

```text
购物与二手交易平台小学期重构
```

3. 开发模式选择 Scrum 或看板。
4. 邀请组员进入项目。
5. 建立以下看板列：

```text
待办 -> 进行中 -> 代码评审 -> 测试中 -> 已完成
```

### 2.3 CodeArts 仓库导入

在 CodeArts Repo 中导入 GitHub 仓库：

```text
https://github.com/tchen-0213/softw.git
```

仓库名称建议为：

```text
softw
```

导入后确认仓库包含：

```text
backend/
frontend/
README.md
部署文档.md
测试文档.md
小学期重构计划.md
CodeArts使用说明.md
```

### 2.4 分支与提交规范

建议分支：

| 分支 | 用途 |
| --- | --- |
| main | 稳定交付分支 |
| dev | 日常集成分支 |
| feature/docs-codearts | CodeArts 与文档完善 |
| feature/security-hardening | 安全加固 |
| feature/performance | 性能优化 |
| feature/testing | 测试体系 |

提交信息建议格式：

```text
docs: 完善小学期 CodeArts 使用说明
test: 补充接口测试用例
perf: 优化商品列表查询
security: 增加上传文件类型限制
refactor: 调整后端模块结构
```

### 2.5 CodeArts 流水线

建议创建 `softw-ci` 流水线，基础步骤如下：

#### 2.5.1 后端依赖安装

```bash
cd backend
npm install
```

#### 2.5.2 前端依赖安装与构建

```bash
cd frontend
npm install
npm run build
```

#### 2.5.3 后续测试步骤

若后端补充自动化测试脚本，可追加：

```bash
cd backend
npm test
```

若前端补充测试脚本，可追加：

```bash
cd frontend
npm test
```

### 2.6 本地测试部署流程

当前稳定路线下，演示环境优先采用本地部署。

后端：

```bash
cd backend
cp .env.example .env
npm install
npm run dev
```

前端：

```bash
cd frontend
npm install
npm run dev
```

数据库：

```sql
CREATE DATABASE shopping_platform DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

初始化演示数据：

```bash
cd backend
npm run seed
npm run seed:scenario
```

### 2.7 可选华为云部署方案

如果后续学校要求必须云端运行，可扩展为：

- 华为云 ECS：部署 Node.js 后端和前端静态资源。
- 华为云 RDS for MySQL：部署数据库。
- CodeArts Pipeline：自动构建。
- CodeArts Deploy：自动发布到 ECS。
- 华为云 AOM/APM：运行监控。

该方案当前作为可选增强项，不作为本阶段最低交付要求。

### 2.8 部署验收材料

最终建议保留：

- CodeArts 项目首页截图。
- CodeArts Repo 仓库截图。
- Scrum 或看板任务截图。
- 流水线执行成功截图。
- 本地前后端启动截图。
- 健康检查接口截图：

```text
http://localhost:3001/api/health
```
