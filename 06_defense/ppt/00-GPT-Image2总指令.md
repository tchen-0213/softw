# 给 Image 2 的说明（复制全文）

## 你要做什么

我们已有一份 **8 页** 初版：`软件工程基础实践-答辩PPT-初版.pptx`（任务完成 → CI/CD → 拆分 → 数据归属 → 测试 → HPA → 故障 → 性能）。

请 **按这 8 页的题目和信息结构** 逐页生成 16:9 幻灯图。排版可以比初版更好看，但不要改成另一套目录，也不要拆成十几页。

这套 PPT 只用于前 **3 分钟项目与架构**。现场演示会播放我们自己的录屏，不要做逐步操作演示页。问答用助教材料，不要做问答页。

第 9 页只要一张很短的转场：分工、大模型、然后播放录屏。一共 **9 页**。

每一页「必须出现」的文字以 `01-逐页文案.md` 为准（已改正过期数字，并补上老师要的追溯）。数字出处见 `02-数字出处.md`。

## 边界（不能破）

1. 数字、哈希、HTTP 状态码、路径 **原样照抄**，不许口算或编新数据。
2. 禁止「微服务架构天然更快」。性能页保留初版口径：限定条件下查询更快，但资源成本更高。
3. 业务微服务 3 个：user / product / order。网关不算。评价、聊天、上传在 product-service。
4. UC01–UC12 全部完成；代表用例 UC01、UC02、UC04。
5. 后端现在是 **220** 通过（不是初版的 80）；E2E 是 **42/42**（不是 6/6）；测试日期 **2026-09-03**（不是 09-02）；当前提交可写 **1895430**（不要再用 c1b73ec）。
6. 前端新增 100 个回归写「Vitest 待复跑」。
7. 权重不编百分比。
8. CI 绿勾是 Actions **#77**（33579985248）。初版第 2 页那张流水线图可以沿用，但不要把别的 run 写成 #77。
9. 模型图若要用仓库 PNG，贴原图，不要重绘。

## 偏好

- 保留初版那种「大标题 + 短副标题 + 大数字/流程块」的信息密度，不要改成大段论文。
- 风格你定，目标是上台能看清。
- 一次一页，从第 1 页开始。
