# 给 Image 2 的说明（复制全文）

## 你要做什么

生成 **16:9** 答辩幻灯（一次一页）。这套 PPT 只讲前 **3 分钟「项目和架构说明」**。现场演示播放录屏，不要做操作演示页；问答用助教材料，不要做问答页。

**目录必须按助教刚发的这段顺序，不要用组里初版 PPT 的顺序。**

初版是按 2026 夏任务书排的：完成情况 → CI/CD → 拆分 → 表归属 → 测试 → HPA → 故障 → 性能。  
助教现在要求的「项目和架构说明」顺序是：

1. 项目目标及全部业务场景完成情况
2. 选取代表性用例，说明需求、设计、代码和测试之间的追溯关系
3. 讲清模型到代码的关系
4. 每个业务微服务的职责、划分依据、接口调用和数据表归属
5. 调用失败时的处理办法
6. 用关键数据概括：单元测试、集成测试、端到端测试、CI/CD、自动扩缩容、故障处理、单体—微服务性能对比

页结构以 `01-逐页文案.md` 为准（封面 + 上列 6 块 + 分工）。不要改回初版目录，也不要做成 18 页。

幻灯正文只写过程、数据、成果、资料。不要写「随后播放录屏」「老师要看」「权重待确认」「不等于只做了三条」。

初版 pptx、`参考-18页带图稿.pptx` 只当「大数字、卡片风」的视觉参考。必须嵌入 `06_defense/配图/` 原图并美化排版。数字以 `02-数字出处.md` 为准。

## 边界（不能破）

1. 数字、哈希、HTTP 状态码、路径原样照抄。
2. 禁止「微服务架构天然更快」。
3. 业务微服务只有 user / product / order；网关不算；评价、聊天、上传在 product-service。
4. UC01–UC12 全部完成；代表用例 UC01、UC02、UC04。
5. 后端 220（不是 80）；E2E 42/42（不是 6/6）；日期 2026-09-03；提交 1895430（不要 c1b73ec）。
6. 前端新增 100 个回归写 Vitest 待复跑。权重不编。
7. CI 绿勾用 `P02-GitHub-Actions-77.png`；带 `20260825` 的图必须标明早期记录。
8. 配图 PNG 贴原图，不要重绘。

## 偏好

每页有图有数，信息铺满，字能看清。风格你定。一次一页，从第 1 页开始。
