把 `00-GPT-Image2总指令.md` 当作说明。

目录必须按助教「项目和架构说明」：目标与全部场景 → 代表用例追溯 → 模型到代码 → 微服务职责/划分/接口/表 → 失败处理 → 关键数据（测试、CI、HPA、故障、性能）。不要用初版那种先 CI 再拆服务的顺序。

请生成第 1 页（封面）。内容只用 `01-逐页文案.md` 的「第 1 页」。可小图 `P00-仓库首页.png`。做完等确认再做第 2 页。
