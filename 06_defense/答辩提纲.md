# 答辩提纲

重点演示用例：**UC01 注册登录、UC02 搜索、UC04 下单支付发货收货**。最终范围 UC01–UC12。

## 给 PPT 的建议页与数字

数字来自已提交报告，制作幻灯片时直接抄，不要口算。

1. 项目：校园购物 + 二手；仓库 https://github.com/tchen-0213/softw
2. 原系统：React → 一个 Express → MySQL `shopping_platform`；标签 `monolith-start`（`10fa639`）
3. 改造：Docker 三容器、GitHub Actions、Kubernetes YAML、三个业务微服务 + 网关；标签 `microservices-v1`（`63585e0`）
4. 划分：user / product / order；库 `softw_users` / `softw_catalog` / `softw_orders`；网关不算业务服务；图 `33-MS-SPLIT`
5. 容器：单体 mysql + backend + frontend（8080 / 3001）；微服务另起一套（前端 8082、网关 8081）
6. CI：工作流 `softw-ci-cd`，测试失败不构建镜像；已验证绿勾 [#77](https://github.com/tchen-0213/softw/actions/runs/33579985248)
7. 测试（2026-09-03）：后端 220/220（含新增 100 个回归用例）、前端原有 100/100 并新增 100 个回归用例待复跑、API 32/32、E2E 单体与微服务均 42/42、网关 API 15/15
8. 压测（三接口各 3 次）：列表吞吐 +61.9%、延迟 −38.6%、CPU +55.2%；**不要写微服务天然更快**
9. HPA：`1 → 3 → 5 → 1`；故障：商品 503、订单 206，其他服务仍存活
10. 账号：`demo-seller@example.com` / `Demo@123456`；本地用 `http://localhost:8080`

老师最新口径：**总共 10 分钟** = 项目和架构 **3 分钟** + 现场演示 **4 分钟** + 问答 **3 分钟**。成品幻灯见 `13组-摸鱼-最终答辩.pptx`，逐页文案和给 GPT 的提示在 `ppt/`。技术总结见 `技术总结报告.md`。

## 3 分钟项目与架构说明

1. 项目：校园购物 + 二手；UC01–UC12 全部完成，现场重点 UC01 / UC02 / UC04。
2. 追溯：需求—三级模型—代码—测试，用 UC04 指到 SYS/COMP/OBJ 和 `orderController`。
3. 三个业务微服务 user / product / order，网关不算业务服务；表归属与 503/206。
4. 关键数据：测试表、Actions #77、列表 +61.9%/−38.6%/+55.2%、HPA `1→3→5→1`、商品 503 订单 206。不要说微服务天然更快。

## 4 分钟现场演示

按老师要求准备可重复过程（可加速录屏，内嵌 PPT，最好配音字幕）：

1. 提交/打开流水线（终期用 Actions #77），说明测试失败不发版。
2. `kubectl -n softw-microservices get pods`。
3. `http://localhost:8080` 走 UC01→UC02→UC04；可加 `E2E-TC04`。
4. HPA 从触发到回落（不要放进 CI/CD）。
5. 停止 product-service，看 503 / 206，其它服务仍在。

## 3 分钟问答准备

- 老师原句：二阶段测试；不必演示全部用例；HPA 全过程不进 CI；压测看 Pod 增减，故障看隔离和备用结果。
- 为什么三服务不是十二服务；评价聊天在商品服务。
- 列表更快但 CPU 更高。
- 本人分工；大模型用途与人工检查。
