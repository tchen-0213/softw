# 购物与二手交易平台软件详细设计说明书

## 1. 引言

### 1.1 文档目的
本文档旨在详细描述购物与二手交易平台的具体实现细节，包括系统的类设计、方法实现、接口定义、数据库操作和业务流程等。文档基于软件概要设计说明书，为开发人员提供具体的实现指导。

### 1.2 术语定义
| 术语 | 解释 |
|------|------|
| 控制器 | 处理HTTP请求，调用服务层方法，返回响应 |
| 服务 | 实现业务逻辑，调用数据访问层方法 |
| 数据访问对象 | 负责数据库操作，实现数据的CRUD操作 |
| 实体 | 对应数据库表结构的Java对象 |
| DTO | 数据传输对象，用于前后端数据传输 |
| 工具类 | 提供通用功能的静态方法 |

## 2. 前端详细设计

### 2.1 目录结构

```
frontend/
├── public/              # 静态资源
├── src/
│   ├── components/      # 组件
│   │   ├── user/        # 用户相关组件
│   │   ├── product/     # 商品相关组件
│   │   ├── order/       # 订单相关组件
│   │   ├── shop/        # 店铺相关组件
│   │   ├── evaluation/  # 评价相关组件
│   │   └── logistics/   # 物流相关组件
│   ├── pages/           # 页面
│   │   ├── home/        # 首页
│   │   ├── search/      # 搜索页
│   │   ├── product/     # 商品详情页
│   │   ├── cart/        # 购物车页
│   │   ├── order/       # 订单页
│   │   ├── user/        # 用户中心
│   │   ├── shop/        # 店铺页
│   │   └──二手/         # 二手交易页
│   ├── services/        # API服务
│   ├── store/           # Redux状态管理
│   ├── utils/           # 工具类
│   ├── styles/          # 样式文件
│   └── App.js           # 应用入口
├── package.json         # 依赖管理
└── README.md            # 项目说明
```

### 2.2 核心组件设计

#### 2.2.1 商品搜索组件

| 组件名称 | 功能描述 | 属性 | 方法 |
|---------|---------|------|------|
| SearchBar | 搜索输入框 | placeholder: string | onSearch: function |
| SearchResults | 搜索结果展示 | results: array | handleProductClick: function |
| FilterPanel | 筛选面板 | filters: object | onFilterChange: function |
| SortBar | 排序栏 | sortOptions: array | onSortChange: function |

#### 2.2.2 商品详情组件

| 组件名称 | 功能描述 | 属性 | 方法 |
|---------|---------|------|------|
| ProductImages | 商品图片展示 | images: array | handleImageClick: function |
| ProductInfo | 商品信息展示 | product: object | - |
| SellerInfo | 卖家信息展示 | seller: object | handleSellerClick: function |
| ProductActions | 商品操作（加入购物车、立即购买） | product: object | handleAddToCart: function, handleBuyNow: function |
| ProductReviews | 商品评价展示 | reviews: array | - |

#### 2.2.3 购物车组件

| 组件名称 | 功能描述 | 属性 | 方法 |
|---------|---------|------|------|
| CartItem | 购物车项 | item: object | handleQuantityChange: function, handleRemove: function |
| CartSummary | 购物车汇总 | items: array | handleCheckout: function |
| CartEmpty | 空购物车提示 | - | handleGoShopping: function |

#### 2.2.4 订单组件

| 组件名称 | 功能描述 | 属性 | 方法 |
|---------|---------|------|------|
| OrderList | 订单列表 | orders: array | handleOrderClick: function |
| OrderItem | 订单项 | order: object | handleCancel: function, handlePay: function, handleReceive: function |
| OrderDetail | 订单详情 | order: object | handleCancel: function, handlePay: function, handleReceive: function, handleEvaluate: function |
| OrderForm | 订单创建表单 | products: array | handleSubmit: function |

#### 2.2.5 二手商品发布组件

| 组件名称 | 功能描述 | 属性 | 方法 |
|---------|---------|------|------|
| ProductForm | 商品信息表单 | initialValues: object | handleSubmit: function |
| ImageUploader | 图片上传 | images: array | handleImageUpload: function, handleImageRemove: function |
| CategorySelect | 分类选择 | categories: array | handleCategoryChange: function |
| ConditionSelect | 成色选择 | - | handleConditionChange: function |

### 2.3 API服务设计

```javascript
// services/api.js
import axios from 'axios';

const API_BASE_URL = 'http://localhost:8080/api';

const api = axios.create({
  baseURL: API_BASE_URL,
  headers: {
    'Content-Type': 'application/json'
  }
});

// 请求拦截器
api.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  error => {
    return Promise.reject(error);
  }
);

// 用户相关API
export const userApi = {
  register: (data) => api.post('/users/register', data),
  login: (data) => api.post('/users/login', data),
  getProfile: () => api.get('/users/profile'),
  updateProfile: (data) => api.put('/users/profile', data)
};

// 商品相关API
export const productApi = {
  getList: (params) => api.get('/products', { params }),
  getDetail: (id) => api.get(`/products/${id}`),
  create: (data) => api.post('/products', data),
  update: (id, data) => api.put(`/products/${id}`, data),
  delete: (id) => api.delete(`/products/${id}`),
  search: (params) => api.get('/products/search', { params })
};

// 订单相关API
export const orderApi = {
  create: (data) => api.post('/orders', data),
  getList: (params) => api.get('/orders', { params }),
  getDetail: (id) => api.get(`/orders/${id}`),
  cancel: (id) => api.put(`/orders/${id}/cancel`),
  pay: (id) => api.put(`/orders/${id}/pay`),
  ship: (id, data) => api.put(`/orders/${id}/ship`, data),
  receive: (id) => api.put(`/orders/${id}/receive`)
};

// 评价相关API
export const evaluationApi = {
  create: (data) => api.post('/evaluations', data),
  getList: (params) => api.get('/evaluations', { params }),
  reply: (id, data) => api.put(`/evaluations/${id}/reply`)
};

// 店铺相关API
export const shopApi = {
  create: (data) => api.post('/shops', data),
  getDetail: (id) => api.get(`/shops/${id}`),
  update: (id, data) => api.put(`/shops/${id}`, data),
  getProducts: (id, params) => api.get(`/shops/${id}/products`, { params })
};

// 物流相关API
export const logisticsApi = {
  getInfo: (orderId) => api.get(`/logistics/order/${orderId}`)
};

export default api;
```

### 2.4 状态管理设计

```javascript
// store/index.js
import { configureStore } from '@reduxjs/toolkit';
import userReducer from './userSlice';
import productReducer from './productSlice';
import orderReducer from './orderSlice';
import cartReducer from './cartSlice';

const store = configureStore({
  reducer: {
    user: userReducer,
    product: productReducer,
    order: orderReducer,
    cart: cartReducer
  }
});

export default store;

// store/userSlice.js
import { createSlice, createAsyncThunk } from '@reduxjs/toolkit';
import { userApi } from '../services/api';

export const register = createAsyncThunk(
  'user/register',
  async (data, { rejectWithValue }) => {
    try {
      const response = await userApi.register(data);
      return response.data;
    } catch (error) {
      return rejectWithValue(error.response.data);
    }
  }
);

export const login = createAsyncThunk(
  'user/login',
  async (data, { rejectWithValue }) => {
    try {
      const response = await userApi.login(data);
      localStorage.setItem('token', response.data.token);
      return response.data;
    } catch (error) {
      return rejectWithValue(error.response.data);
    }
  }
);

export const getProfile = createAsyncThunk(
  'user/getProfile',
  async (_, { rejectWithValue }) => {
    try {
      const response = await userApi.getProfile();
      return response.data;
    } catch (error) {
      return rejectWithValue(error.response.data);
    }
  }
);

const userSlice = createSlice({
  name: 'user',
  initialState: {
    isAuthenticated: false,
    user: null,
    loading: false,
    error: null
  },
  reducers: {
    logout: (state) => {
      state.isAuthenticated = false;
      state.user = null;
      localStorage.removeItem('token');
    }
  },
  extraReducers: (builder) => {
    builder
      .addCase(register.pending, (state) => {
        state.loading = true;
        state.error = null;
      })
      .addCase(register.fulfilled, (state, action) => {
        state.loading = false;
        state.isAuthenticated = true;
        state.user = action.payload.user;
      })
      .addCase(register.rejected, (state, action) => {
        state.loading = false;
        state.error = action.payload;
      })
      .addCase(login.pending, (state) => {
        state.loading = true;
        state.error = null;
      })
      .addCase(login.fulfilled, (state, action) => {
        state.loading = false;
        state.isAuthenticated = true;
        state.user = action.payload.user;
      })
      .addCase(login.rejected, (state, action) => {
        state.loading = false;
        state.error = action.payload;
      })
      .addCase(getProfile.pending, (state) => {
        state.loading = true;
        state.error = null;
      })
      .addCase(getProfile.fulfilled, (state, action) => {
        state.loading = false;
        state.isAuthenticated = true;
        state.user = action.payload;
      })
      .addCase(getProfile.rejected, (state, action) => {
        state.loading = false;
        state.isAuthenticated = false;
        state.user = null;
        state.error = action.payload;
      });
  }
});

export const { logout } = userSlice.actions;
export default userSlice.reducer;
```

## 3. 后端详细设计

### 3.1 目录结构

```
backend/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/
│   │   │       └── example/
│   │   │           └── shopping/
│   │   │               ├── controller/    # 控制器
│   │   │               ├── service/       # 服务
│   │   │               ├── repository/    # 数据访问
│   │   │               ├── entity/        # 实体
│   │   │               ├── dto/           # 数据传输对象
│   │   │               ├── config/        # 配置
│   │   │               ├── security/      # 安全
│   │   │               ├── utils/         # 工具类
│   │   │               └── Application.java # 应用入口
│   │   └── resources/
│   │       ├── application.yml          # 配置文件
│   │       └── static/                  # 静态资源
│   └── test/                            # 测试
├── pom.xml                              # 依赖管理
└── README.md                            # 项目说明
```

### 3.2 控制器设计

#### 3.2.1 用户控制器

| 类名 | 方法 | URL | 功能描述 | 请求体 | 响应体 |
|------|------|-----|---------|--------|--------|
| UserController | register | POST /api/users/register | 用户注册 | UserRegisterDTO | {"token": "...", "user": {...}} |
| UserController | login | POST /api/users/login | 用户登录 | UserLoginDTO | {"token": "...", "user": {...}} |
| UserController | getProfile | GET /api/users/profile | 获取个人信息 | N/A | UserDTO |
| UserController | updateProfile | PUT /api/users/profile | 更新个人信息 | UserUpdateDTO | UserDTO |
| UserController | getAddressList | GET /api/users/addresses | 获取地址列表 | N/A | List<AddressDTO> |
| UserController | addAddress | POST /api/users/addresses | 添加地址 | AddressDTO | AddressDTO |
| UserController | updateAddress | PUT /api/users/addresses/{id} | 更新地址 | AddressDTO | AddressDTO |
| UserController | deleteAddress | DELETE /api/users/addresses/{id} | 删除地址 | N/A | {"success": true} |

#### 3.2.2 商品控制器

| 类名 | 方法 | URL | 功能描述 | 请求体 | 响应体 |
|------|------|-----|---------|--------|--------|
| ProductController | getList | GET /api/products | 获取商品列表 | N/A | List<ProductDTO> |
| ProductController | getDetail | GET /api/products/{id} | 获取商品详情 | N/A | ProductDTO |
| ProductController | create | POST /api/products | 创建商品 | ProductCreateDTO | ProductDTO |
| ProductController | update | PUT /api/products/{id} | 更新商品 | ProductUpdateDTO | ProductDTO |
| ProductController | delete | DELETE /api/products/{id} | 删除商品 | N/A | {"success": true} |
| ProductController | search | GET /api/products/search | 搜索商品 | N/A | List<ProductDTO> |
| ProductController | getRecommended | GET /api/products/recommended | 获取推荐商品 | N/A | List<ProductDTO> |

#### 3.2.3 订单控制器

| 类名 | 方法 | URL | 功能描述 | 请求体 | 响应体 |
|------|------|-----|---------|--------|--------|
| OrderController | create | POST /api/orders | 创建订单 | OrderCreateDTO | OrderDTO |
| OrderController | getList | GET /api/orders | 获取订单列表 | N/A | List<OrderDTO> |
| OrderController | getDetail | GET /api/orders/{id} | 获取订单详情 | N/A | OrderDTO |
| OrderController | cancel | PUT /api/orders/{id}/cancel | 取消订单 | N/A | OrderDTO |
| OrderController | pay | PUT /api/orders/{id}/pay | 支付订单 | N/A | OrderDTO |
| OrderController | ship | PUT /api/orders/{id}/ship | 发货 | OrderShipDTO | OrderDTO |
| OrderController | receive | PUT /api/orders/{id}/receive | 收货 | N/A | OrderDTO |
| OrderController | refund | POST /api/orders/{id}/refund | 申请退款 | RefundDTO | OrderDTO |

#### 3.2.4 评价控制器

| 类名 | 方法 | URL | 功能描述 | 请求体 | 响应体 |
|------|------|-----|---------|--------|--------|
| EvaluationController | create | POST /api/evaluations | 创建评价 | EvaluationCreateDTO | EvaluationDTO |
| EvaluationController | getList | GET /api/evaluations | 获取评价列表 | N/A | List<EvaluationDTO> |
| EvaluationController | getByUserId | GET /api/evaluations/user/{userId} | 获取用户评价 | N/A | List<EvaluationDTO> |
| EvaluationController | reply | PUT /api/evaluations/{id}/reply | 回复评价 | EvaluationReplyDTO | EvaluationDTO |

#### 3.2.5 店铺控制器

| 类名 | 方法 | URL | 功能描述 | 请求体 | 响应体 |
|------|------|-----|---------|--------|--------|
| ShopController | create | POST /api/shops | 创建店铺 | ShopCreateDTO | ShopDTO |
| ShopController | getDetail | GET /api/shops/{id} | 获取店铺详情 | N/A | ShopDTO |
| ShopController | update | PUT /api/shops/{id} | 更新店铺 | ShopUpdateDTO | ShopDTO |
| ShopController | getProducts | GET /api/shops/{id}/products | 获取店铺商品 | N/A | List<ProductDTO> |
| ShopController | getStatistics | GET /api/shops/{id}/statistics | 获取店铺统计 | N/A | ShopStatisticsDTO |

#### 3.2.6 物流控制器

| 类名 | 方法 | URL | 功能描述 | 请求体 | 响应体 |
|------|------|-----|---------|--------|--------|
| LogisticsController | getInfo | GET /api/logistics/order/{orderId} | 获取订单物流信息 | N/A | LogisticsDTO |
| LogisticsController | updateStatus | PUT /api/logistics/{id}/status | 更新物流状态 | LogisticsStatusDTO | LogisticsDTO |

### 3.3 服务设计

#### 3.3.1 用户服务

```java
@Service
public class UserService {
    @Autowired
    private UserRepository userRepository;
    
    @Autowired
    private PasswordEncoder passwordEncoder;
    
    @Autowired
    private JwtTokenProvider jwtTokenProvider;
    
    public UserDTO register(UserRegisterDTO dto) {
        // 检查用户名是否已存在
        if (userRepository.existsByUsername(dto.getUsername())) {
            throw new BadRequestException("用户名已存在");
        }
        
        // 检查手机号是否已存在
        if (userRepository.existsByPhone(dto.getPhone())) {
            throw new BadRequestException("手机号已存在");
        }
        
        // 创建用户
        User user = new User();
        user.setUsername(dto.getUsername());
        user.setPassword(passwordEncoder.encode(dto.getPassword()));
        user.setPhone(dto.getPhone());
        user.setEmail(dto.getEmail());
        user.setNickname(dto.getNickname());
        user.setGender(dto.getGender());
        user.setBirthday(dto.getBirthday());
        user.setCreditScore(100);
        user.setCreditLevel("普通会员");
        
        user = userRepository.save(user);
        
        return UserMapper.INSTANCE.toDTO(user);
    }
    
    public LoginResponseDTO login(UserLoginDTO dto) {
        // 查找用户
        User user = userRepository.findByUsername(dto.getUsername())
                .orElseThrow(() -> new UnauthorizedException("用户名或密码错误"));
        
        // 验证密码
        if (!passwordEncoder.matches(dto.getPassword(), user.getPassword())) {
            throw new UnauthorizedException("用户名或密码错误");
        }
        
        // 生成token
        String token = jwtTokenProvider.generateToken(user.getId());
        
        return new LoginResponseDTO(token, UserMapper.INSTANCE.toDTO(user));
    }
    
    public UserDTO getProfile(Integer userId) {
        User user = userRepository.findById(userId)
                .orElseThrow(() -> new NotFoundException("用户不存在"));
        
        return UserMapper.INSTANCE.toDTO(user);
    }
    
    public UserDTO updateProfile(Integer userId, UserUpdateDTO dto) {
        User user = userRepository.findById(userId)
                .orElseThrow(() -> new NotFoundException("用户不存在"));
        
        // 更新用户信息
        if (dto.getNickname() != null) {
            user.setNickname(dto.getNickname());
        }
        if (dto.getGender() != null) {
            user.setGender(dto.getGender());
        }
        if (dto.getBirthday() != null) {
            user.setBirthday(dto.getBirthday());
        }
        if (dto.getAvatar() != null) {
            user.setAvatar(dto.getAvatar());
        }
        
        user = userRepository.save(user);
        
        return UserMapper.INSTANCE.toDTO(user);
    }
    
    public void updateCreditScore(Integer userId, int score) {
        User user = userRepository.findById(userId)
                .orElseThrow(() -> new NotFoundException("用户不存在"));
        
        user.setCreditScore(user.getCreditScore() + score);
        
        // 更新信用等级
        if (user.getCreditScore() >= 150) {
            user.setCreditLevel("钻石会员");
        } else if (user.getCreditScore() >= 120) {
            user.setCreditLevel("黄金会员");
        } else if (user.getCreditScore() >= 100) {
            user.setCreditLevel("普通会员");
        } else {
            user.setCreditLevel("待提升");
        }
        
        userRepository.save(user);
    }
}
```

#### 3.3.2 商品服务

```java
@Service
public class ProductService {
    @Autowired
    private ProductRepository productRepository;
    
    @Autowired
    private CategoryRepository categoryRepository;
    
    @Autowired
    private UserRepository userRepository;
    
    public List<ProductDTO> getList(ProductQueryDTO query) {
        Pageable pageable = PageRequest.of(query.getPage() - 1, query.getSize());
        Page<Product> page = productRepository.findAll(query.toSpecification(), pageable);
        
        return page.getContent().stream()
                .map(ProductMapper.INSTANCE::toDTO)
                .collect(Collectors.toList());
    }
    
    public ProductDTO getDetail(Integer id) {
        Product product = productRepository.findById(id)
                .orElseThrow(() -> new NotFoundException("商品不存在"));
        
        return ProductMapper.INSTANCE.toDTO(product);
    }
    
    public ProductDTO create(Integer sellerId, ProductCreateDTO dto) {
        // 验证分类是否存在
        Category category = categoryRepository.findById(dto.getCategoryId())
                .orElseThrow(() -> new BadRequestException("分类不存在"));
        
        // 验证卖家是否存在
        User seller = userRepository.findById(sellerId)
                .orElseThrow(() -> new NotFoundException("卖家不存在"));
        
        // 创建商品
        Product product = new Product();
        product.setName(dto.getName());
        product.setDescription(dto.getDescription());
        product.setImages(JSON.toJSONString(dto.getImages()));
        product.setVideo(dto.getVideo());
        product.setPrice(dto.getPrice());
        product.setStock(dto.getStock());
        product.setCategoryId(dto.getCategoryId());
        product.setSellerId(sellerId);
        product.setProductType(dto.getProductType());
        product.setCondition(dto.getCondition());
        product.setStatus(1); // 在售
        
        product = productRepository.save(product);
        
        return ProductMapper.INSTANCE.toDTO(product);
    }
    
    public ProductDTO update(Integer id, Integer sellerId, ProductUpdateDTO dto) {
        Product product = productRepository.findById(id)
                .orElseThrow(() -> new NotFoundException("商品不存在"));
        
        // 验证是否是商品卖家
        if (!product.getSellerId().equals(sellerId)) {
            throw new ForbiddenException("无权限修改此商品");
        }
        
        // 更新商品信息
        if (dto.getName() != null) {
            product.setName(dto.getName());
        }
        if (dto.getDescription() != null) {
            product.setDescription(dto.getDescription());
        }
        if (dto.getImages() != null) {
            product.setImages(JSON.toJSONString(dto.getImages()));
        }
        if (dto.getVideo() != null) {
            product.setVideo(dto.getVideo());
        }
        if (dto.getPrice() != null) {
            product.setPrice(dto.getPrice());
        }
        if (dto.getStock() != null) {
            product.setStock(dto.getStock());
        }
        if (dto.getCategoryId() != null) {
            product.setCategoryId(dto.getCategoryId());
        }
        if (dto.getCondition() != null) {
            product.setCondition(dto.getCondition());
        }
        if (dto.getStatus() != null) {
            product.setStatus(dto.getStatus());
        }
        
        product = productRepository.save(product);
        
        return ProductMapper.INSTANCE.toDTO(product);
    }
    
    public void delete(Integer id, Integer sellerId) {
        Product product = productRepository.findById(id)
                .orElseThrow(() -> new NotFoundException("商品不存在"));
        
        // 验证是否是商品卖家
        if (!product.getSellerId().equals(sellerId)) {
            throw new ForbiddenException("无权限删除此商品");
        }
        
        productRepository.delete(product);
    }
    
    public List<ProductDTO> search(String keyword, Integer page, Integer size) {
        Pageable pageable = PageRequest.of(page - 1, size);
        Page<Product> pageResult = productRepository.search(keyword, pageable);
        
        return pageResult.getContent().stream()
                .map(ProductMapper.INSTANCE::toDTO)
                .collect(Collectors.toList());
    }
    
    public List<ProductDTO> getRecommended(Integer userId, Integer limit) {
        // 基于用户历史行为推荐商品
        // 这里简化实现，实际应该使用更复杂的推荐算法
        List<Product> products = productRepository.findTopByStatusOrderByCreatedAtDesc(1, limit);
        
        return products.stream()
                .map(ProductMapper.INSTANCE::toDTO)
                .collect(Collectors.toList());
    }
}
```

#### 3.3.3 订单服务

```java
@Service
@Transactional
public class OrderService {
    @Autowired
    private OrderRepository orderRepository;
    
    @Autowired
    private ProductRepository productRepository;
    
    @Autowired
    private UserRepository userRepository;
    
    @Autowired
    private EvaluationRepository evaluationRepository;
    
    public OrderDTO create(Integer buyerId, OrderCreateDTO dto) {
        // 验证商品是否存在
        Product product = productRepository.findById(dto.getProductId())
                .orElseThrow(() -> new NotFoundException("商品不存在"));
        
        // 验证商品是否在售
        if (product.getStatus() != 1) {
            throw new BadRequestException("商品已下架或已售出");
        }
        
        // 验证库存
        if (product.getStock() < dto.getQuantity()) {
            throw new BadRequestException("商品库存不足");
        }
        
        // 验证买家是否存在
        User buyer = userRepository.findById(buyerId)
                .orElseThrow(() -> new NotFoundException("买家不存在"));
        
        // 验证卖家是否存在
        User seller = userRepository.findById(product.getSellerId())
                .orElseThrow(() -> new NotFoundException("卖家不存在"));
        
        // 扣减库存
        product.setStock(product.getStock() - dto.getQuantity());
        if (product.getStock() == 0) {
            product.setStatus(2); // 已售出
        }
        productRepository.save(product);
        
        // 创建订单
        Order order = new Order();
        order.setBuyerId(buyerId);
        order.setSellerId(product.getSellerId());
        order.setProductId(dto.getProductId());
        order.setQuantity(dto.getQuantity());
        order.setTotalAmount(product.getPrice().multiply(BigDecimal.valueOf(dto.getQuantity())));
        order.setStatus(1); // 待支付
        order.setCreatedAt(new Date());
        
        order = orderRepository.save(order);
        
        return OrderMapper.INSTANCE.toDTO(order);
    }
    
    public List<OrderDTO> getList(Integer userId, Integer role, Integer status, Integer page, Integer size) {
        Pageable pageable = PageRequest.of(page - 1, size);
        Page<Order> pageResult;
        
        if (role == 1) { // 买家
            pageResult = orderRepository.findByBuyerIdAndStatus(userId, status, pageable);
        } else if (role == 2) { // 卖家
            pageResult = orderRepository.findBySellerIdAndStatus(userId, status, pageable);
        } else {
            throw new BadRequestException("角色无效");
        }
        
        return pageResult.getContent().stream()
                .map(OrderMapper.INSTANCE::toDTO)
                .collect(Collectors.toList());
    }
    
    public OrderDTO getDetail(Integer id, Integer userId) {
        Order order = orderRepository.findById(id)
                .orElseThrow(() -> new NotFoundException("订单不存在"));
        
        // 验证是否是订单买家或卖家
        if (!order.getBuyerId().equals(userId) && !order.getSellerId().equals(userId)) {
            throw new ForbiddenException("无权限查看此订单");
        }
        
        return OrderMapper.INSTANCE.toDTO(order);
    }
    
    public OrderDTO cancel(Integer id, Integer userId) {
        Order order = orderRepository.findById(id)
                .orElseThrow(() -> new NotFoundException("订单不存在"));
        
        // 验证是否是订单买家
        if (!order.getBuyerId().equals(userId)) {
            throw new ForbiddenException("无权限取消此订单");
        }
        
        // 验证订单状态
        if (order.getStatus() != 1) { // 待支付
            throw new BadRequestException("订单状态不正确");
        }
        
        // 更新订单状态
        order.setStatus(5); // 已取消
        orderRepository.save(order);
        
        // 恢复库存
        Product product = productRepository.findById(order.getProductId())
                .orElseThrow(() -> new NotFoundException("商品不存在"));
        product.setStock(product.getStock() + order.getQuantity());
        product.setStatus(1); // 在售
        productRepository.save(product);
        
        return OrderMapper.INSTANCE.toDTO(order);
    }
    
    public OrderDTO pay(Integer id, Integer userId) {
        Order order = orderRepository.findById(id)
                .orElseThrow(() -> new NotFoundException("订单不存在"));
        
        // 验证是否是订单买家
        if (!order.getBuyerId().equals(userId)) {
            throw new ForbiddenException("无权限支付此订单");
        }
        
        // 验证订单状态
        if (order.getStatus() != 1) { // 待支付
            throw new BadRequestException("订单状态不正确");
        }
        
        // 更新订单状态
        order.setStatus(2); // 待发货
        order.setPaidAt(new Date());
        orderRepository.save(order);
        
        return OrderMapper.INSTANCE.toDTO(order);
    }
    
    public OrderDTO ship(Integer id, Integer userId, OrderShipDTO dto) {
        Order order = orderRepository.findById(id)
                .orElseThrow(() -> new NotFoundException("订单不存在"));
        
        // 验证是否是订单卖家
        if (!order.getSellerId().equals(userId)) {
            throw new ForbiddenException("无权限发货此订单");
        }
        
        // 验证订单状态
        if (order.getStatus() != 2) { // 待发货
            throw new BadRequestException("订单状态不正确");
        }
        
        // 更新订单状态
        order.setStatus(3); // 待收货
        order.setShippedAt(new Date());
        order.setLogisticsNumber(dto.getLogisticsNumber());
        orderRepository.save(order);
        
        return OrderMapper.INSTANCE.toDTO(order);
    }
    
    public OrderDTO receive(Integer id, Integer userId) {
        Order order = orderRepository.findById(id)
                .orElseThrow(() -> new NotFoundException("订单不存在"));
        
        // 验证是否是订单买家
        if (!order.getBuyerId().equals(userId)) {
            throw new ForbiddenException("无权限收货此订单");
        }
        
        // 验证订单状态
        if (order.getStatus() != 3) { // 待收货
            throw new BadRequestException("订单状态不正确");
        }
        
        // 更新订单状态
        order.setStatus(4); // 已完成
        order.setReceivedAt(new Date());
        orderRepository.save(order);
        
        return OrderMapper.INSTANCE.toDTO(order);
    }
}
```

#### 3.3.4 评价服务

```java
@Service
public class EvaluationService {
    @Autowired
    private EvaluationRepository evaluationRepository;
    
    @Autowired
    private OrderRepository orderRepository;
    
    @Autowired
    private UserService userService;
    
    public EvaluationDTO create(Integer evaluatorId, EvaluationCreateDTO dto) {
        // 验证订单是否存在
        Order order = orderRepository.findById(dto.getOrderId())
                .orElseThrow(() -> new NotFoundException("订单不存在"));
        
        // 验证是否是订单买家
        if (!order.getBuyerId().equals(evaluatorId)) {
            throw new ForbiddenException("无权限评价此订单");
        }
        
        // 验证订单状态
        if (order.getStatus() != 4) { // 已完成
            throw new BadRequestException("订单未完成，无法评价");
        }
        
        // 验证是否已评价
        if (evaluationRepository.existsByOrderId(dto.getOrderId())) {
            throw new BadRequestException("此订单已评价");
        }
        
        // 创建评价
        Evaluation evaluation = new Evaluation();
        evaluation.setOrderId(dto.getOrderId());
        evaluation.setEvaluatorId(evaluatorId);
        evaluation.setEvaluatedId(order.getSellerId());
        evaluation.setScore(dto.getScore());
        evaluation.setContent(dto.getContent());
        evaluation.setCreatedAt(new Date());
        
        evaluation = evaluationRepository.save(evaluation);
        
        // 更新卖家信用评分
        userService.updateCreditScore(order.getSellerId(), dto.getScore() - 3); // 评分5-1，转换为-2到+2
        
        return EvaluationMapper.INSTANCE.toDTO(evaluation);
    }
    
    public List<EvaluationDTO> getList(Integer evaluatedId, Integer page, Integer size) {
        Pageable pageable = PageRequest.of(page - 1, size);
        Page<Evaluation> pageResult = evaluationRepository.findByEvaluatedId(evaluatedId, pageable);
        
        return pageResult.getContent().stream()
                .map(EvaluationMapper.INSTANCE::toDTO)
                .collect(Collectors.toList());
    }
    
    public EvaluationDTO reply(Integer id, Integer userId, EvaluationReplyDTO dto) {
        Evaluation evaluation = evaluationRepository.findById(id)
                .orElseThrow(() -> new NotFoundException("评价不存在"));
        
        // 验证是否是被评价人
        if (!evaluation.getEvaluatedId().equals(userId)) {
            throw new ForbiddenException("无权限回复此评价");
        }
        
        // 更新回复
        evaluation.setReply(dto.getReply());
        evaluation = evaluationRepository.save(evaluation);
        
        return EvaluationMapper.INSTANCE.toDTO(evaluation);
    }
}
```

### 3.4 数据访问设计

#### 3.4.1 用户数据访问

```java
@Repository
public interface UserRepository extends JpaRepository<User, Integer> {
    Optional<User> findByUsername(String username);
    boolean existsByUsername(String username);
    boolean existsByPhone(String phone);
    boolean existsByEmail(String email);
}
```

#### 3.4.2 商品数据访问

```java
@Repository
public interface ProductRepository extends JpaRepository<Product, Integer>, JpaSpecificationExecutor<Product> {
    Page<Product> findByStatusOrderByCreatedAtDesc(Integer status, Pageable pageable);
    Page<Product> search(String keyword, Pageable pageable);
    List<Product> findBySellerId(Integer sellerId);
    List<Product> findByCategoryId(Integer categoryId);
}

// 商品搜索实现
public class ProductRepositoryImpl implements ProductRepositoryCustom {
    @Autowired
    private EntityManager entityManager;
    
    @Override
    public Page<Product> search(String keyword, Pageable pageable) {
        CriteriaBuilder cb = entityManager.getCriteriaBuilder();
        CriteriaQuery<Product> cq = cb.createQuery(Product.class);
        Root<Product> root = cq.from(Product.class);
        
        // 构建搜索条件
        Predicate predicate = cb.or(
            cb.like(root.get("name"), "%" + keyword + "%"),
            cb.like(root.get("description"), "%" + keyword + "%")
        );
        cq.where(predicate);
        
        // 构建排序
        if (pageable.getSort().isSorted()) {
            List<Order> orders = new ArrayList<>();
            for (Sort.Order order : pageable.getSort()) {
                orders.add(cb.order(root.get(order.getProperty()), order.getDirection().isAscending() ? SortOrder.ASC : SortOrder.DESC));
            }
            cq.orderBy(orders);
        } else {
            cq.orderBy(cb.desc(root.get("createdAt")));
        }
        
        // 执行查询
        TypedQuery<Product> query = entityManager.createQuery(cq);
        query.setFirstResult((int) pageable.getOffset());
        query.setMaxResults(pageable.getPageSize());
        
        // 获取总记录数
        CriteriaQuery<Long> countQuery = cb.createQuery(Long.class);
        countQuery.select(cb.count(countQuery.from(Product.class)));
        countQuery.where(predicate);
        long total = entityManager.createQuery(countQuery).getSingleResult();
        
        List<Product> content = query.getResultList();
        return new PageImpl<>(content, pageable, total);
    }
}
```

#### 3.4.3 订单数据访问

```java
@Repository
public interface OrderRepository extends JpaRepository<Order, Integer> {
    Page<Order> findByBuyerIdAndStatus(Integer buyerId, Integer status, Pageable pageable);
    Page<Order> findBySellerIdAndStatus(Integer sellerId, Integer status, Pageable pageable);
    List<Order> findByProductId(Integer productId);
    List<Order> findByBuyerId(Integer buyerId);
    List<Order> findBySellerId(Integer sellerId);
}
```

#### 3.4.4 评价数据访问

```java
@Repository
public interface EvaluationRepository extends JpaRepository<Evaluation, Integer> {
    boolean existsByOrderId(Integer orderId);
    Page<Evaluation> findByEvaluatedId(Integer evaluatedId, Pageable pageable);
    List<Evaluation> findByEvaluatorId(Integer evaluatorId);
}
```

#### 3.4.5 店铺数据访问

```java
@Repository
public interface ShopRepository extends JpaRepository<Shop, Integer> {
    Optional<Shop> findBySellerId(Integer sellerId);
    boolean existsBySellerId(Integer sellerId);
    boolean existsByName(String name);
}
```

### 3.5 安全设计

#### 3.5.1 JWT认证

```java
@Component
public class JwtTokenProvider {
    @Value("${jwt.secret}")
    private String secretKey;
    
    @Value("${jwt.expiration}")
    private long expirationTime;
    
    public String generateToken(Integer userId) {
        Date now = new Date();
        Date expiryDate = new Date(now.getTime() + expirationTime);
        
        return Jwts.builder()
                .setSubject(Long.toString(userId))
                .setIssuedAt(now)
                .setExpiration(expiryDate)
                .signWith(SignatureAlgorithm.HS512, secretKey)
                .compact();
    }
    
    public Integer getUserIdFromToken(String token) {
        Claims claims = Jwts.parser()
                .setSigningKey(secretKey)
                .parseClaimsJws(token)
                .getBody();
        
        return Integer.parseInt(claims.getSubject());
    }
    
    public boolean validateToken(String token) {
        try {
            Jwts.parser().setSigningKey(secretKey).parseClaimsJws(token);
            return true;
        } catch (SignatureException | MalformedJwtException | ExpiredJwtException | UnsupportedJwtException | IllegalArgumentException e) {
            return false;
        }
    }
}

@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {
    @Autowired
    private JwtTokenProvider tokenProvider;
    
    @Autowired
    private UserDetailsService userDetailsService;
    
    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response, FilterChain filterChain) throws ServletException, IOException {
        try {
            String token = getJwtFromRequest(request);
            
            if (token != null && tokenProvider.validateToken(token)) {
                Integer userId = tokenProvider.getUserIdFromToken(token);
                UserDetails userDetails = userDetailsService.loadUserById(userId);
                UsernamePasswordAuthenticationToken authentication = new UsernamePasswordAuthenticationToken(userDetails, null, userDetails.getAuthorities());
                authentication.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));
                SecurityContextHolder.getContext().setAuthentication(authentication);
            }
        } catch (Exception ex) {
            logger.error("Could not set user authentication in security context", ex);
        }
        
        filterChain.doFilter(request, response);
    }
    
    private String getJwtFromRequest(HttpServletRequest request) {
        String bearerToken = request.getHeader("Authorization");
        if (StringUtils.hasText(bearerToken) && bearerToken.startsWith("Bearer ")) {
            return bearerToken.substring(7);
        }
        return null;
    }
}
```

#### 3.5.2 权限控制

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig extends WebSecurityConfigurerAdapter {
    @Autowired
    private JwtAuthenticationFilter jwtAuthenticationFilter;
    
    @Autowired
    private CustomAccessDeniedHandler accessDeniedHandler;
    
    @Autowired
    private CustomAuthenticationEntryPoint authenticationEntryPoint;
    
    @Override
    protected void configure(HttpSecurity http) throws Exception {
        http
            .cors()
                .and()
            .csrf()
                .disable()
            .exceptionHandling()
                .accessDeniedHandler(accessDeniedHandler)
                .authenticationEntryPoint(authenticationEntryPoint)
                .and()
            .sessionManagement()
                .sessionCreationPolicy(SessionCreationPolicy.STATELESS)
                .and()
            .authorizeRequests()
                .antMatchers("/api/users/register", "/api/users/login").permitAll()
                .antMatchers("/api/products", "/api/products/{id}", "/api/products/search").permitAll()
                .antMatchers("/api/shops/{id}", "/api/shops/{id}/products").permitAll()
                .anyRequest().authenticated();
        
        http.addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class);
    }
    
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}

@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(UnauthorizedException.class)
    public ResponseEntity<ErrorResponse> handleUnauthorizedException(UnauthorizedException ex) {
        ErrorResponse error = new ErrorResponse(401, ex.getMessage());
        return new ResponseEntity<>(error, HttpStatus.UNAUTHORIZED);
    }
    
    @ExceptionHandler(ForbiddenException.class)
    public ResponseEntity<ErrorResponse> handleForbiddenException(ForbiddenException ex) {
        ErrorResponse error = new ErrorResponse(403, ex.getMessage());
        return new ResponseEntity<>(error, HttpStatus.FORBIDDEN);
    }
    
    @ExceptionHandler(NotFoundException.class)
    public ResponseEntity<ErrorResponse> handleNotFoundException(NotFoundException ex) {
        ErrorResponse error = new ErrorResponse(404, ex.getMessage());
        return new ResponseEntity<>(error, HttpStatus.NOT_FOUND);
    }
    
    @ExceptionHandler(BadRequestException.class)
    public ResponseEntity<ErrorResponse> handleBadRequestException(BadRequestException ex) {
        ErrorResponse error = new ErrorResponse(400, ex.getMessage());
        return new ResponseEntity<>(error, HttpStatus.BAD_REQUEST);
    }
    
    @ExceptionHandler(Exception.class)
    public ResponseEntity<ErrorResponse> handleException(Exception ex) {
        ErrorResponse error = new ErrorResponse(500, "服务器内部错误");
        return new ResponseEntity<>(error, HttpStatus.INTERNAL_SERVER_ERROR);
    }
}
```

## 4. 数据库设计

### 4.1 数据库表结构

#### 4.1.1 用户表（users）
| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 用户ID |
| username | VARCHAR(50) | NOT NULL, UNIQUE | 用户名 |
| password | VARCHAR(100) | NOT NULL | 密码（加密存储） |
| phone | VARCHAR(20) | UNIQUE | 手机号 |
| email | VARCHAR(100) | UNIQUE | 邮箱 |
| avatar | VARCHAR(255) | | 头像URL |
| nickname | VARCHAR(50) | | 昵称 |
| gender | TINYINT | | 性别（0-未知，1-男，2-女） |
| birthday | DATE | | 生日 |
| registration_time | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 注册时间 |
| credit_score | INT | NOT NULL, DEFAULT 100 | 信用评分 |
| credit_level | VARCHAR(20) | NOT NULL, DEFAULT '普通会员' | 信用等级 |

#### 4.1.2 商品表（products）
| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 商品ID |
| name | VARCHAR(100) | NOT NULL | 商品名称 |
| description | TEXT | | 商品描述 |
| images | TEXT | | 商品图片URL（JSON格式） |
| video | VARCHAR(255) | | 商品视频URL |
| price | DECIMAL(10,2) | NOT NULL | 价格 |
| stock | INT | NOT NULL, DEFAULT 0 | 库存 |
| category_id | INT | NOT NULL, FOREIGN KEY | 分类ID |
| seller_id | INT | NOT NULL, FOREIGN KEY | 卖家ID |
| product_type | TINYINT | NOT NULL | 商品类型（1-新品，2-二手） |
| condition | TINYINT | | 成色（仅二手商品，1-全新，2-九成新，3-八成新，4-七成新，5-六成新及以下） |
| created_at | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 上架时间 |
| status | TINYINT | NOT NULL, DEFAULT 1 | 状态（1-在售，2-已售出，3-下架） |

#### 4.1.3 订单表（orders）
| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 订单ID |
| buyer_id | INT | NOT NULL, FOREIGN KEY | 买家ID |
| seller_id | INT | NOT NULL, FOREIGN KEY | 卖家ID |
| product_id | INT | NOT NULL, FOREIGN KEY | 商品ID |
| quantity | INT | NOT NULL, DEFAULT 1 | 商品数量 |
| total_amount | DECIMAL(10,2) | NOT NULL | 订单金额 |
| status | TINYINT | NOT NULL, DEFAULT 1 | 订单状态（1-待支付，2-待发货，3-待收货，4-已完成，5-已取消，6-退款中，7-已退款） |
| created_at | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| paid_at | DATETIME | | 支付时间 |
| shipped_at | DATETIME | | 发货时间 |
| received_at | DATETIME | | 收货时间 |
| logistics_number | VARCHAR(50) | | 物流单号 |

#### 4.1.4 评价表（evaluations）
| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 评价ID |
| order_id | INT | NOT NULL, FOREIGN KEY, UNIQUE | 订单ID |
| evaluator_id | INT | NOT NULL, FOREIGN KEY | 评价人ID |
| evaluated_id | INT | NOT NULL, FOREIGN KEY | 被评价人ID |
| score | TINYINT | NOT NULL | 评分（1-5星） |
| content | TEXT | | 评价内容 |
| created_at | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 评价时间 |
| reply | TEXT | | 回复内容 |

#### 4.1.5 店铺表（shops）
| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 店铺ID |
| name | VARCHAR(100) | NOT NULL | 店铺名称 |
| avatar | VARCHAR(255) | | 店铺头像URL |
| description | TEXT | | 店铺简介 |
| seller_id | INT | NOT NULL, FOREIGN KEY, UNIQUE | 卖家ID |
| created_at | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| status | TINYINT | NOT NULL, DEFAULT 1 | 店铺状态（1-正常，2-关闭） |

#### 4.1.6 商品分类表（categories）
| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 分类ID |
| name | VARCHAR(50) | NOT NULL | 分类名称 |
| parent_id | INT | | 父分类ID |
| level | TINYINT | NOT NULL, DEFAULT 1 | 分类级别 |

#### 4.1.7 地址表（addresses）
| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 地址ID |
| user_id | INT | NOT NULL, FOREIGN KEY | 用户ID |
| name | VARCHAR(50) | NOT NULL | 收货人姓名 |
| phone | VARCHAR(20) | NOT NULL | 收货人手机号 |
| province | VARCHAR(50) | NOT NULL | 省份 |
| city | VARCHAR(50) | NOT NULL | 城市 |
| district | VARCHAR(50) | NOT NULL | 区县 |
| detail | VARCHAR(255) | NOT NULL | 详细地址 |
| is_default | TINYINT | NOT NULL, DEFAULT 0 | 是否默认地址 |

### 4.2 索引设计

| 表名 | 索引名 | 索引字段 | 类型 | 用途 |
|------|--------|---------|------|------|
| users | idx_username | username | 唯一索引 | 加速用户名查询 |
| users | idx_phone | phone | 唯一索引 | 加速手机号查询 |
| users | idx_email | email | 唯一索引 | 加速邮箱查询 |
| products | idx_seller_id | seller_id | 普通索引 | 加速卖家商品查询 |
| products | idx_category_id | category_id | 普通索引 | 加速分类商品查询 |
| products | idx_status | status | 普通索引 | 加速状态商品查询 |
| products | idx_created_at | created_at | 普通索引 | 加速时间排序 |
| orders | idx_buyer_id | buyer_id | 普通索引 | 加速买家订单查询 |
| orders | idx_seller_id | seller_id | 普通索引 | 加速卖家订单查询 |
| orders | idx_product_id | product_id | 普通索引 | 加速商品订单查询 |
| orders | idx_status | status | 普通索引 | 加速状态订单查询 |
| evaluations | idx_order_id | order_id | 唯一索引 | 加速订单评价查询 |
| evaluations | idx_evaluated_id | evaluated_id | 普通索引 | 加速被评价人评价查询 |
| shops | idx_seller_id | seller_id | 唯一索引 | 加速卖家店铺查询 |
| addresses | idx_user_id | user_id | 普通索引 | 加速用户地址查询 |

## 5. 部署与集成方案

### 5.1 环境配置

#### 5.1.1 前端环境
- Node.js 14.0+
- npm 6.0+
- React 18.0+
- Ant Design 5.0+

#### 5.1.2 后端环境
- Java 11+
- Spring Boot 2.7+
- MySQL 8.0+
- Redis 6.0+

### 5.2 部署步骤

#### 5.2.1 前端部署
1. 安装依赖：`npm install`
2. 构建项目：`npm run build`
3. 将构建产物部署到CDN或静态文件服务器

#### 5.2.2 后端部署
1. 编译项目：`mvn clean package`
2. 部署jar包：`java -jar shopping-1.0.0.jar`
3. 配置环境变量：
   - `DB_HOST`: 数据库主机
   - `DB_PORT`: 数据库端口
   - `DB_NAME`: 数据库名称
   - `DB_USER`: 数据库用户名
   - `DB_PASSWORD`: 数据库密码
   - `REDIS_HOST`: Redis主机
   - `REDIS_PORT`: Redis端口
   - `JWT_SECRET`: JWT密钥
   - `JWT_EXPIRATION`: JWT过期时间

### 5.3 集成测试

#### 5.3.1 API测试
- 使用Postman或Swagger测试API接口
- 测试用户注册、登录、商品管理、订单管理等功能
- 验证API响应状态码和数据格式

#### 5.3.2 前端测试
- 使用Cypress或Selenium测试前端功能
- 测试页面加载、交互功能、表单提交等
- 验证前端与后端API的交互

#### 5.3.3 性能测试
- 使用JMeter或Gatling测试系统性能
- 测试并发用户数、响应时间、吞吐量等
- 验证系统在高负载下的稳定性

## 6. 总结

本详细设计说明书详细描述了购物与二手交易平台的具体实现细节，包括前端和后端的代码结构、接口设计、数据库设计和部署方案。系统采用前后端分离的微服务架构，实现了商品搜索与浏览、在线下单与支付、二手商品发布与交易、信用评价体系、个人店铺管理和物流跟踪等核心功能。系统将使用现代的技术栈和开发方法，注重性能优化和用户体验，打造一个安全、便捷、可信赖的线上交易生态。