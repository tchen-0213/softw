# 软件概要设计说明书

> 1. 《软件概要设计说明书》\(SDD\) 概要设计说明书又可称系统设计说明书，这里所说的系统是指程序系统。编制的目的是说明对程序系统的设计考虑，包括程序系统的基本处理流程、程序系统的组织结构、模块划分、功能分配、接口设计、运行设计、安全设计、数据结构设计和出错处理设计等，为程序的详细设计提供基础。
> 
> 2. 本课程及本实验不涉及、不强调、不关注的部分可省略，但不允许删除已有条目。
> 
> 3. 最终文档请删除所有灰色字体和斜体等说明部分。
> 
> 

\[Toc\]

## 1 引言

### 1\.1 编写目的

本文档旨在详细描述购物与二手交易平台的概要设计，包括系统架构、模块划分、数据结构设计和关键技术实现方案。文档基于软件需求规格说明书，为系统的详细设计和实现提供指导。预期读者为系统开发人员、测试人员、项目管理人员等。

### 1\.2 背景

a．待开发软件系统的名称：购物与二手交易平台；
b．本项目的任务提出者为电商业务需求方，开发者为软件研发团队，用户为有购物及二手交易需求的个人用户 / 商家，将运行该软件的计算站为云服务器集群（包含应用服务器、数据库服务器、缓存服务器等）。

### 1\.3 定义

|术语|解释|
|---|---|
|前端|系统的用户界面部分，负责与用户交互|
|后端|系统的服务器端部分，负责业务逻辑处理和数据存储|
|API|应用程序接口，用于前后端通信|
|数据库|存储系统数据的仓库|
|微服务|一种软件架构风格，将应用程序拆分为多个独立的服务|
|JWT|JSON Web Token，一种用于身份验证的令牌|
|Redis|一种内存数据库，用于缓存和会话管理|

### 1\.4 参考设计

- 软件需求规格说明书（购物与二手交易平台）

- 相关技术官方文档（Spring Boot、React、MySQL、Redis 等）

- 行业内电商平台通用设计规范

## 2 系统需求概述

### 2\.1 需求规定

#### 2\.1\.1 输入项目

- 用户注册 / 登录信息（用户名、密码、手机号、邮箱等）

- 商品发布信息（名称、描述、图片 / 视频、价格、库存、分类等）

- 订单操作信息（商品选择、数量、支付方式、收货地址等）

- 评价信息（评分、文字内容、回复内容等）

- 店铺管理信息（店铺名称、简介、头像等）

#### 2\.1\.2 输出项目

- 商品列表 / 详情展示数据

- 订单创建 / 支付 / 物流状态数据

- 用户个人信息 / 信用评分数据

- 店铺运营 / 商品管理数据

- 评价展示 / 信用等级数据

#### 2\.1\.3 处理的功能性能要求

- 高并发处理能力：支持峰值 10 万 \+ 用户同时在线，订单创建响应时间≤500ms

- 数据查询性能：商品搜索结果返回时间≤100ms，订单查询响应时间≤200ms

- 系统可用性：全年可用性≥99\.9%，故障恢复时间≤30 分钟

- 数据一致性：订单、支付、库存数据实时一致，无脏数据 / 重复数据

### 2\.2 业务目标

打造安全、便捷、可信赖的购物与二手交易线上生态，实现商品搜索与浏览、在线下单与支付、二手商品发布与交易、信用评价体系、个人店铺管理和物流跟踪等核心业务闭环。

### 2\.3 运行环境

#### 2\.3\.1 设备

- 服务器：云服务器（CPU≥8 核，内存≥16GB，磁盘≥500GB）、容器集群（Kubernetes）

- 存储设备：云数据库存储、OSS 对象存储（图片 / 视频）、Redis 缓存集群

- 网络设备：高带宽网络链路、负载均衡设备、防火墙设备

#### 2\.3\.2 支持软件

- 操作系统：Linux（CentOS/Ubuntu）

- 编译 / 运行环境：JDK 1\.8\+、Node\.js 16\+

- 框架 / 中间件：Spring Boot、React、Redux、Ant Design、MySQL、Redis、RabbitMQ、Elasticsearch

- 测试支持软件：Junit、Postman、JMeter

- 运维支持软件：Jenkins、Prometheus、Grafana、ELK stack

### 2\.4 设计约束

- 采用前后端分离的微服务架构，需遵循微服务设计规范

- 需兼容主流浏览器（Chrome、Firefox、Safari、Edge）及移动端设备

- 数据存储需满足隐私保护法规，用户敏感信息加密存储

- 第三方接口（支付、物流）需遵循对接规范，保证稳定性

- 系统需支持横向扩展，满足业务增量需求

### 2\.5 功能需求

#### 2\.5\.1 基本设计概念

本系统采用前后端分离的微服务架构，将系统分为前端层、API 层、服务层、数据层四层结构，各层解耦且职责明确：

- 前端层负责用户交互与界面展示；

- API 层负责请求接收与转发，统一接口规范；

- 服务层按业务拆分为独立微服务，处理核心业务逻辑；

- 数据层负责数据存储与缓存，保证数据安全与性能。

#### 2\.5\.2 核心处理流程

##### 2\.5\.2\.1 商品购买流程

1\.  用户操作：已登录用户浏览商品详情，选择商品数量后点击“加入购物车”或“立即购买”，进入结算页面；未登录用户需先完成登录/注册，再进行结算操作。

2\.  数据校验：前端校验商品库存、用户登录状态，后端接口校验商品状态（是否在售）、库存数量、用户权限，确保数据合法。

3\.  订单创建：用户确认收货地址、支付方式后提交订单，后端订单服务创建订单记录，关联商品、买家、卖家信息，生成唯一订单ID，同时扣减对应商品库存。

4\.  支付对接：系统跳转至第三方支付接口（支付宝/微信支付），用户完成支付操作，支付平台回调系统接口，更新订单支付状态。

5\.  流程闭环：支付成功后，订单状态更新为“待发货”，卖家收到发货通知；卖家发货后更新订单状态为“待收货”，买家确认收货后订单状态更新为“已完成”，完成整个购买流程。

##### 2\.5\.2\.2 二手商品发布流程

1\.  用户操作：已登录用户进入二手商品发布页面，填写商品基本信息（名称、描述、价格）、选择商品分类、上传商品图片/视频，设置商品成色（全新至六成新及以下）。

2\.  信息校验：前端校验商品信息完整性（必填项是否填写）、图片/视频格式及大小，后端校验用户信用评分（需满足最低信用要求）、商品信息合法性（无违规内容）。

3\.  数据存储：校验通过后，商品服务将商品信息存储至数据库，商品图片/视频上传至OSS存储，生成商品ID，关联卖家信息，设置商品类型为“二手”、状态为“在售”。

4\.  索引同步：商品信息同步至Elasticsearch，建立全文搜索索引，支持用户通过关键词搜索该二手商品。

5\.  发布完成：系统提示用户发布成功，跳转至商品详情页，同时将商品推送至二手商品推荐列表，完成发布流程。

### 2\.6 非功能需求

- 安全性：用户密码加密存储、接口请求签名验证、JWT 身份认证、SSL 数据传输加密

- 可扩展性：微服务架构支持服务独立扩容，数据库支持分库分表，前端组件化支持功能扩展

- 易用性：前端界面符合用户操作习惯，响应式设计适配多终端，操作流程简洁易懂

- 可维护性：代码模块化、接口规范化、日志完善化，支持服务独立部署 / 升级

- 兼容性：兼容主流浏览器（Chrome 80\+、Firefox 75\+、Safari 13\+、Edge 80\+），移动端适配 iOS 10\+、Android 8\+

### 2\.7 系统总体架构

#### 2\.7\.1 架构层次结构

```Plain Text
┌───────────────────────────────────────────────────────────────┐
│                           前端层                             │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌───────────┐ │
│ │  用户界面   │ │  商品界面   │ │  订单界面   │ │  店铺界面  │ │
│ └─────────────┘ └─────────────┘ └─────────────┘ └───────────┘ │
├───────────────────────────────────────────────────────────────┤
│                           API层                              │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌───────────┐ │
│ │ 用户API     │ │ 商品API     │ │ 订单API     │ │ 店铺API   │ │
│ └─────────────┘ └─────────────┘ └─────────────┘ └───────────┘ │
├───────────────────────────────────────────────────────────────┤
│                         服务层                               │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌───────────┐ │
│ │ 用户服务    │ │ 商品服务    │ │ 订单服务    │ │ 店铺服务  │ │
│ └─────────────┘ └─────────────┘ └─────────────┘ └───────────┘ │
├───────────────────────────────────────────────────────────────┤
│                         数据层                               │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌───────────┐ │
│ │ 用户数据    │ │ 商品数据    │ │ 订单数据    │ │ 店铺数据  │ │
│ └─────────────┘ └─────────────┘ └─────────────┘ └───────────┘ │
└───────────────────────────────────────────────────────────────┘
```

#### 2\.7\.2 架构说明

本系统采用前后端分离的微服务架构，前端层负责用户交互与界面展示，API 层统一接收前端请求并转发至对应服务层，服务层按业务拆分为用户、商品、订单、店铺等独立微服务，数据层负责数据的存储与管理，各层通过标准化接口通信，保证系统解耦与可扩展。

## 3 系统接口设计

### 3\.2 接口设计

#### 3\.2\.1 用户接口

##### \[3\.2\.1\.1\]\(3\.2\.1\.1\) 命令 / 操作接口（前端操作）

|操作命令|语法 / 操作方式|系统反馈信息|
|---|---|---|
|用户注册|填写用户名、密码、手机号 / 邮箱，点击 “注册” 按钮|注册成功 / 失败（提示失败原因：用户名已存在 / 手机号格式错误等）|
|用户登录|输入用户名 / 手机号 \+ 密码，点击 “登录” 按钮|登录成功（跳转首页）/ 失败（提示账号密码错误 / 账号未激活等）|
|商品搜索|在搜索框输入关键词，点击 “搜索”/ 回车|展示搜索结果列表（无结果时提示 “未找到相关商品”）|
|商品发布|填写商品信息，上传图片，点击 “发布” 按钮|发布成功（跳转商品列表）/ 失败（提示信息缺失 / 权限不足等）|
|订单创建|选择商品数量，点击 “结算”，填写收货地址，选择支付方式|订单创建成功（跳转支付页）/ 失败（提示库存不足 / 地址为空等）|

##### \[3\.2\.1\.2\]\(3\.2\.1\.2\) 硬件控制面板（无专用硬件控制面板，基于通用终端操作）

- 电脑端：通过鼠标 / 键盘操作浏览器界面，支持快捷键（如回车搜索、ESC 关闭弹窗）

- 移动端：通过触屏操作（点击、滑动、长按），支持手势操作（如下拉刷新、上拉加载）

#### 3\.2\.2 与其他软件、硬件接口

##### \[3\.2\.2\.1\]\(3\.2\.2\.1\) 软件接口

|对接软件 / 系统|接口类型|接口关系 / 交互方式|
|---|---|---|
|支付宝 / 微信支付|RESTful API|后端服务层调用第三方支付 API，传递订单信息，接收支付结果回调|
|快递公司物流系统|RESTful API|后端物流服务调用快递公司 API，查询物流轨迹，接收物流状态更新|
|Elasticsearch|内部 API|商品服务调用 Elasticsearch API，实现商品全文搜索|
|Redis|内部 API|各服务层调用 Redis API，缓存热点数据（商品、用户、订单）|
|RabbitMQ|消息队列接口|服务层之间通过 RabbitMQ 异步通信（如订单创建后通知库存服务扣减库存）|

##### \[3\.2\.2\.2\]\(3\.2\.2\.2\) 硬件接口

|对接硬件|接口类型|交互方式|
|---|---|---|
|云服务器|TCP/IP|通过网络协议部署应用程序，实现服务运行与数据传输|
|OSS 存储设备|HTTP/HTTPS|前端 / 后端通过 API 上传 / 下载商品图片 / 视频，存储至 OSS|
|负载均衡设备|TCP/IP|分发前端请求至多个 API 层节点，实现负载分摊|

### 3\.3 系统数据结构设计

#### 3\.3\.1 逻辑结构设计要点

##### \[3\.3\.1\.1\]\(3\.3\.1\.1\) 用户表（users）

|标识符|名称|定义|长度|层次 / 关系|
|---|---|---|---|---|
|id|用户 ID|唯一标识用户|INT|主键|
|username|用户名|用户登录账号|VARCHAR\(50\)|非空、唯一|
|password|密码|加密存储的用户密码|VARCHAR\(100\)|非空|
|phone|手机号|用户联系电话|VARCHAR\(20\)|唯一|
|email|邮箱|用户邮箱|VARCHAR\(100\)|唯一|
|avatar|头像 URL|用户头像存储地址|VARCHAR\(255\)|\-|
|nickname|昵称|用户显示名称|VARCHAR\(50\)|\-|
|gender|性别|0 \- 未知，1 \- 男，2 \- 女|TINYINT|\-|
|birthday|生日|用户出生日期|DATE|\-|
|registration\_time|注册时间|用户注册时间|DATETIME|非空、默认当前时间|
|credit\_score|信用评分|用户初始评分 100|INT|非空、默认 100|
|credit\_level|信用等级|基于评分的等级|VARCHAR\(20\)|非空、默认普通会员|

##### \[3\.3\.1\.2\]\(3\.3\.1\.2\) 商品表（products）

|标识符|名称|定义|长度|层次 / 关系|
|---|---|---|---|---|
|id|商品 ID|唯一标识商品|INT|主键|
|name|商品名称|商品展示名称|VARCHAR\(100\)|非空|
|description|商品描述|商品详情说明|TEXT|\-|
|images|商品图片 URL|JSON 格式存储多图地址|TEXT|\-|
|video|商品视频 URL|商品视频存储地址|VARCHAR\(255\)|\-|
|price|价格|商品售价|DECIMAL\(10,2\)|非空|
|stock|库存|商品可售数量|INT|非空、默认 0|
|category\_id|分类 ID|关联商品分类表|INT|非空、外键|
|seller\_id|卖家 ID|关联用户表|INT|非空、外键|
|product\_type|商品类型|1 \- 新品，2 \- 二手|TINYINT|非空|
|condition|成色|仅二手商品，1 \- 全新至 5 \- 六成新及以下|TINYINT|\-|
|上架时间|上架时间|商品上架时间|DATETIME|非空、默认当前时间|
|status|状态|1 \- 在售，2 \- 已售出，3 \- 下架|TINYINT|非空、默认 1|

##### \[3\.3\.1\.3\]\(3\.3\.1\.3\) 订单表（orders）

|标识符|名称|定义|长度|层次 / 关系|
|---|---|---|---|---|
|id|订单 ID|唯一标识订单|INT|主键|
|buyer\_id|买家 ID|关联用户表|INT|非空、外键|
|seller\_id|卖家 ID|关联用户表|INT|非空、外键|
|product\_id|商品 ID|关联商品表|INT|非空、外键|
|quantity|商品数量|订单商品数量|INT|非空、默认 1|
|total\_amount|订单金额|订单总金额|DECIMAL\(10,2\)|非空|
|status|订单状态|1 \- 待支付至 7 \- 已退款|TINYINT|非空、默认 1|
|created\_at|创建时间|订单创建时间|DATETIME|非空、默认当前时间|
|paid\_at|支付时间|订单支付时间|DATETIME|\-|
|shipped\_at|发货时间|订单发货时间|DATETIME|\-|
|received\_at|收货时间|订单收货时间|DATETIME|\-|
|logistics\_number|物流单号|订单物流编号|VARCHAR\(50\)|\-|

##### \[3\.3\.1\.4\]\(3\.3\.1\.4\) 其他核心表（评价表、店铺表、商品分类表）

（逻辑结构设计要点同上述表结构，包含标识符、名称、定义、长度、层次 / 关系等维度，具体字段见本文档 4\.1\.4\-4\.1\.6）

#### 3\.3\.2 物理结构设计要点

##### \[3\.3\.2\.1\]\(3\.3\.2\.1\) 用户表（users）

|数据项|存储要求|访问方法|存取单位|物理关系|设计考虑|保密条件|
|---|---|---|---|---|---|---|
|id|整数存储，自增|主键查询|行|主键索引|保证唯一性，支持快速查询|无|
|username|字符串存储|等值查询|行|唯一索引|登录高频查询，创建唯一索引|无|
|password|字符串存储（加密）|等值查询|行|普通索引|加密算法采用 MD5 \+ 盐值，防止破解|高（加密存储，禁止明文传输）|
|phone/email|字符串存储|等值查询|行|唯一索引|用于登录 / 找回密码，创建唯一索引|中（脱敏展示，仅本人可见）|
|credit\_score/credit\_level|数值 / 字符串存储|等值 / 范围查询|行|普通索引|信用体系核心字段，支持快速查询|中（仅本人 / 平台管理员可见）|

##### \[3\.3\.2\.2\]\(3\.3\.2\.2\) 商品表（products）

|数据项|存储要求|访问方法|存取单位|物理关系|设计考虑|保密条件|
|---|---|---|---|---|---|---|
|id|整数存储，自增|主键查询|行|主键索引|保证唯一性，支持快速查询|无|
|name|字符串存储|模糊查询|行|全文索引|商品搜索高频字段，接入 Elasticsearch|无|
|price|小数存储|范围查询|行|普通索引|价格筛选高频场景，创建索引|无|
|seller\_id|整数存储|等值查询|行|普通索引|店铺商品查询，创建索引|无|
|status|整数存储|等值查询|行|普通索引|筛选在售 / 下架商品，创建索引|无|

##### \[3\.3\.2\.3\]\(3\.3\.2\.3\) 订单表（orders）

|数据项|存储要求|访问方法|存取单位|物理关系|设计考虑|保密条件|
|---|---|---|---|---|---|---|
|id|整数存储，自增|主键查询|行|主键索引|保证唯一性，支持快速查询|无|
|buyer\_id/seller\_id|整数存储|等值查询|行|复合索引|按买家 / 卖家查询订单，创建复合索引|中（仅本人 / 平台管理员可见）|
|status|整数存储|等值查询|行|普通索引|筛选不同状态订单，创建索引|无|
|created\_at|时间存储|范围查询|行|普通索引|按时间筛选订单，创建索引|无|
|total\_amount|小数存储|范围查询|行|普通索引|按金额筛选订单，创建索引|中（仅本人 / 平台管理员可见）|

#### 3\.3\.3 数据结构与程序关系

|数据结构（表）|关联程序 / 服务|交互方式|操作类型|
|---|---|---|---|
|users|用户服务|数据库 CRUD|新增（注册）、查询（登录）、更新（信息修改）、删除（账号注销）|
|products|商品服务|数据库 CRUD \+ Elasticsearch 查询|新增（发布）、查询（搜索 / 详情）、更新（编辑）、删除（下架）|
|orders|订单服务|数据库 CRUD \+ 事务操作|新增（创建）、查询（列表 / 详情）、更新（状态变更）、删除（取消）|
|evaluations|评价服务|数据库 CRUD|新增（提交）、查询（展示）、更新（回复）、删除（审核下架）|
|shops|店铺服务|数据库 CRUD|新增（创建）、查询（详情）、更新（管理）、删除（关闭）|
|categories|商品服务|数据库 CRUD|新增（分类创建）、查询（分类列表）、更新（分类编辑）、删除（分类删除）|

## 4 系统数据库设计

### 4\.1 数据库选型

采用 MySQL 作为核心关系型数据库，存储结构化业务数据；Redis 作为内存数据库，缓存热点数据（商品列表、用户会话、订单状态）；OSS 存储非结构化数据（商品图片 / 视频）。

### 4\.2 数据库部署

- MySQL：主从架构，主库负责写操作，从库负责读操作，实现读写分离；

- Redis：集群部署，主从复制 \+ 哨兵模式，保证高可用；

- 分库分表：按业务模块分库（用户库、商品库、订单库），按数据量分表（订单表按时间分表）。

### 4\.3 数据库优化

- 索引优化：为高频查询字段创建索引，定期重建索引，避免索引失效；

- 缓存策略：Redis 缓存热点数据，设置合理过期时间，实现缓存预热与失效降级；

- SQL 优化：避免全表扫描，优化 JOIN 语句，使用批量操作减少数据库交互。

## 5 运行设计

### 5\.1 运行模块组合

|运行场景|模块组合|说明|
|---|---|---|
|用户注册 / 登录|前端用户模块 \+ 用户 API \+ 用户服务 \+ 用户数据层|完成用户身份验证与信息存储|
|商品浏览 / 搜索|前端商品模块 \+ 商品 API \+ 商品服务 \+ 商品数据层 \+ Elasticsearch|实现商品数据查询与展示|
|商品发布（二手）|前端二手交易模块 \+ 商品 API \+ 商品服务 \+ 商品数据层 \+ OSS|完成商品信息存储与媒体文件上传|
|订单创建 / 支付|前端订单模块 \+ 订单 API \+ 订单服务 \+ 订单数据层 \+ 支付服务|实现订单创建、支付对接与状态更新|
|评价提交 / 查看|前端评价模块 \+ 评价 API \+ 评价服务 \+ 评价数据层|完成评价存储与展示，更新信用评分|
|店铺管理|前端店铺模块 \+ 店铺 API \+ 店铺服务 \+ 店铺数据层|实现店铺信息管理与商品关联|

### 5\.2 运行控制

#### 5\.2\.1 前端控制

- 页面路由控制：基于 React Router 实现前端路由，控制页面跳转；

- 状态控制：基于 Redux 管理全局状态（用户登录状态、购物车状态）；

- 交互控制：表单校验、按钮防重复点击、加载状态提示。

#### 5\.2\.2 后端控制

- 请求控制：API 层统一拦截请求，验证身份（JWT）、参数校验、权限检查；

- 服务控制：微服务之间通过 Eureka 实现服务发现，Ribbon 实现负载均衡，Hystrix 实现熔断降级；

- 数据控制：事务控制（订单创建 \+ 库存扣减）、数据一致性校验、并发控制（分布式锁）。

#### 5\.2\.3 异常控制

- 前端异常：捕获接口请求异常，展示友好提示（网络错误、服务不可用等）；

- 后端异常：全局异常处理器，统一返回异常码与信息，记录异常日志；

- 降级控制：核心服务不可用时，降级为基础功能（如商品搜索降级为仅展示缓存数据）。

### 5\.3 运行时间

#### 5\.3\.1 响应时间

|功能模块|平均响应时间|峰值响应时间|
|---|---|---|
|用户登录|≤200ms|≤500ms|
|商品搜索|≤100ms|≤300ms|
|订单创建|≤500ms|≤1000ms|
|支付结果回调|≤100ms|≤200ms|
|物流信息查询|≤300ms|≤800ms|

#### 5\.3\.2 资源占用

- 前端：单页面内存占用≤200MB，首屏加载时间≤3s；

- 后端 API：单请求 CPU 占用≤5%，内存占用≤10MB；

- 微服务：单服务实例 CPU 占用≤30%，内存占用≤512MB；

- 数据库：查询 CPU 占用≤40%，磁盘 IO≤100MB/s。

#### 5\.3\.3 运行优先级

|优先级|模块|说明|
|---|---|---|
|高|订单服务、支付服务|核心交易流程，保障可用性|
|中|用户服务、商品服务|基础功能，允许短暂降级|
|低|评价服务、物流服务|辅助功能，可临时不可用|

