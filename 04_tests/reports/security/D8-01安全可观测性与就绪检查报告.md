# D8-01 安全、可观测性与就绪检查报告

- 验证日期：2026-09-01
- 代码版本：`033d506349e05fb2fd2edca8ec8f3483e6d67d61`
- 验证环境：Docker Compose 隔离项目 `softw-d801`，MySQL 8.0，Node.js 20/24，Playwright Chromium
- 结论：D8-01 验收项全部通过。

## 已完成内容

1. 网关、用户、商品和订单服务统一接收或生成 `X-Request-Id`，使用 `AsyncLocalStorage` 在跨服务请求中自动传递。
2. 请求和异常日志统一为 JSON；仅记录请求方法、无查询参数路径、状态码、耗时和请求标识，不记录请求正文、密码、Token、内部凭据或错误堆栈。
3. 所有组件区分 `/live`、`/ready`、`/health` 和 `/version`；Kubernetes 分别配置 startup、liveness 和 readiness probe。
4. 网关启用安全响应头、精确来源 CORS、限流、2 MiB 请求体上限和 3 秒上游超时；内部凭据不接受客户端覆盖。
5. 图片上传同时限制 MIME、扩展名、内容签名、单文件大小、文件数量、字段/part 数量，并以 UUID 写入固定目录。
6. JWT、内部服务令牌和数据库密码只从环境变量、Compose 注入或 Kubernetes Secret 获取；生产模式拒绝缺失、默认或过短值。
7. CI 新增生产依赖扫描、工作区密钥扫描、Git 历史 Gitleaks 扫描和扫描报告 artifact。
8. 四个容器均以非 root 用户运行；Kubernetes 禁止提权并移除全部 Linux capabilities。

## 验证结果

| 验证项 | 结果 |
| --- | --- |
| 主后端单元/控制器/安全测试 | 61 通过，0 失败，1 个需真实数据库的套件按设计跳过 |
| 前端单元测试 | 93/93 通过 |
| 用户、商品、订单服务真实 MySQL 测试 | 1/1、3/3、1/1 通过 |
| 网关及公共组件测试 | 13/13 通过 |
| 微服务公开 API 与 UC01–UC09 | 13/13 通过，0 个公开 API 未覆盖 |
| Playwright UC01–UC12 | 6/6 通过 |
| 前端生产构建 | 通过 |
| Compose 配置解析与 JavaScript 语法检查 | 通过 |
| 生产依赖高危/严重漏洞 | 6 个工作区均为 0/0 |
| 纳入版本控制文件的密钥扫描 | 449 个文件，0 条发现 |

依赖扫描仍报告 Sequelize 间接依赖 `uuid` 的 2 条中危记录，影响主后端及三个数据库服务；当前上游 Sequelize 6.37.8 尚未提供兼容升级路径，`npm audit` 仅建议降级到 Sequelize 3.x。该项不属于高危或严重漏洞，已如实保留在原始报告中，后续跟踪上游修复。

## 关键验收证据

- [跨四组件 Trace ID 日志](./d8-01-trace-evidence-2026-09-01.json)：同一个 `requestId` 出现在网关、用户、商品、订单服务；脚本同时断言日志不含密码、授权头、Token 或堆栈。
- [数据库中断与自动恢复](./d8-01-readiness-recovery-2026-09-01.json)：正常时全部 `/live=200`、`/ready=200`；停库后全部 `/live=200`、`/ready=503`；重启数据库后无需重启服务自动恢复为 `/ready=200`。
- [微服务 API JUnit 报告](./d8-01-microservices-e2e-2026-09-01.xml)：公开 API 和 UC01–UC09 共 13 项全部通过。
- [生产依赖与密钥扫描](./security-scan-2026-09-01.json)：所有工作区高危/严重漏洞为 0，密钥发现为 0。

浏览器截图、流水线截图和看板截图按课程提交约定另存云盘，不提交到 GitHub。
