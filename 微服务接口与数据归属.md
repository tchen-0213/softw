# 微服务接口与数据归属

## 服务划分

```text
frontend
  |
api-gateway
  |-- user-service
  |-- product-service
  |-- order-service
  |-- evaluation-service（规划）
  |-- chat-service（规划）
```

本次已落地代码骨架包括 `api-gateway`、`user-service`、`product-service`、`order-service`。评价和聊天服务因时间成本较高，先在单体中保留，作为后续拆分项记录。

## 接口清单

| 服务 | 方法 | 路径 | 说明 |
| --- | --- | --- | --- |
| api-gateway | GET | `/health` | 网关健康检查 |
| api-gateway | GET | `/version` | 网关版本与路由 |
| api-gateway | ALL | `/api/users/**` | 转发到用户服务 |
| api-gateway | ALL | `/api/products/**` | 转发到商品服务 |
| api-gateway | ALL | `/api/secondhand/**` | 转发到商品服务 |
| api-gateway | ALL | `/api/orders/**` | 转发到订单服务 |
| user-service | GET | `/health` | 用户服务健康检查 |
| user-service | GET | `/version` | 用户服务版本 |
| user-service | GET | `/api/users` | 用户列表演示接口 |
| user-service | GET | `/api/users/:id` | 用户详情演示接口 |
| product-service | GET | `/health` | 商品服务健康检查 |
| product-service | GET | `/version` | 商品服务版本 |
| product-service | GET | `/api/products` | 商品列表演示接口 |
| product-service | GET | `/api/products/:id` | 商品详情演示接口 |
| product-service | GET | `/api/secondhand` | 二手商品演示接口 |
| order-service | GET | `/health` | 订单服务健康检查 |
| order-service | GET | `/version` | 订单服务版本 |
| order-service | GET | `/api/orders` | 订单列表演示接口 |
| order-service | GET | `/api/orders/health/dependencies` | 商品服务依赖健康与降级结果 |

## 数据表归属

| 表名 | 归属服务 | 访问规则 |
| --- | --- | --- |
| `users` | user-service | 仅用户服务直接读写 |
| `addresses` | user-service | 仅用户服务直接读写 |
| `products` | product-service | 仅商品服务直接读写 |
| `shops` | product-service | 商品/店铺域统一管理 |
| `orders` | order-service | 仅订单服务直接读写 |
| `evaluations` | evaluation-service | 后续拆出，当前由单体评价模块管理 |
| `chat_conversations` | chat-service | 后续拆出，当前由单体聊天模块管理 |
| `chat_messages` | chat-service | 后续拆出，当前由单体聊天模块管理 |

## 跨服务调用和失败处理

| 场景 | 调用方式 | 失败处理 |
| --- | --- | --- |
| 订单查询商品依赖 | order-service 调用 product-service `/health` 或商品接口 | 返回 `degraded` 和备用提示，订单核心查询继续可用 |
| 前端访问后端 | frontend 调用 api-gateway `/api/**` | 网关超时后返回 503 和统一提示 |
| 商品/二手路由 | api-gateway 按路径转发 product-service | 依赖失败时网关阻断，不影响用户/订单服务 |

## Kubernetes 部署

微服务部署文件位于 `k8s/microservices`，包括：

- 3 个业务服务 Deployment/Service。
- 1 个 API 网关 Deployment/Service。
- `product-service-hpa` 自动扩缩容配置。
- 每个服务的 readinessProbe。
